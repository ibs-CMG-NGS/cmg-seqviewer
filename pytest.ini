[pytest]
pythonpath = src
testpaths = test
markers =
    network: real network calls (enabled explicitly with `-m network`)
    offline: simulations with network blocked (block_network fixture)
    qt: pytest-qt GUI tests
addopts = -ra -m "not network"