# 환경 변수 설정 파일
# .env 파일은 가상환경 활성화 시 자동으로 로드됩니다

# Python 경로
PYTHONPATH=src

# 로그 레벨 (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=DEBUG

# 로그 디렉토리
LOG_DIR=logs

# GUI 설정
QT_AUTO_SCREEN_SCALE_FACTOR=1
QT_ENABLE_HIGHDPI_SCALING=1

# 개발 모드 플래그
DEV_MODE=true
